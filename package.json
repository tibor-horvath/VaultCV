{
  "name": "cv-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "sync:swa-config": "node scripts/sync-swa-config.mjs",
    "test": "npm run test --prefix web && npm run test --prefix api"
  }
}

